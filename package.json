{
  "name": "qwen-proxy",
  "displayName": "Qwen Proxy",
  "description": "OpenAI-compatible API proxy for Qwen web interface",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "qwen-proxy.start",
        "title": "Start Qwen Proxy Server"
      },
      {
        "command": "qwen-proxy.stop",
        "title": "Stop Qwen Proxy Server"
      },
      {
        "command": "qwen-proxy.status",
        "title": "Check Qwen Proxy Status"
      }
    ],
    "configuration": {
      "title": "Qwen Proxy",
      "properties": {
        "qwen-proxy.port": {
          "type": "number",
          "default": 3001,
          "description": "Port for the proxy server"
        },
        "qwen-proxy.qwenUrl": {
          "type": "string",
          "default": "https://qwen.alibaba.com",
          "description": "Qwen web interface URL"
        },
        "qwen-proxy.autoStart": {
          "type": "boolean",
          "default": true,
          "description": "Automatically start proxy server when VSCode opens"
        },
        "qwen-proxy.headless": {
          "type": "boolean",
          "default": true,
          "description": "Run browser in headless mode"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "18.x",
    "@types/express": "^4.17.21",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "@vscode/test-cli": "^0.0.4",
    "@vscode/test-electron": "^2.3.8",
    "eslint": "^8.54.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.6.1",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  }
}